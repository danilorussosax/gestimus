{
  "name": "gestimus",
  "version": "2.0.0",
  "private": true,
  "description": "Gestionale concorso musicale — frontend statico + backend Postgres/Fastify/Drizzle (server/).",
  "scripts": {
    "start": "cd server && npm run dev",
    "test:unit": "node --test 'tests/unit/**/*.test.mjs'",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "autocannon": "^8.0.0"
  }
}
