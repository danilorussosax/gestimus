{
  "name": "gestimus",
  "version": "2.0.0",
  "private": true,
  "description": "Gestionale concorso musicale — SPA React (frontend/) + backend Postgres/Fastify/Drizzle (server/).",
  "scripts": {
    "start": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "autocannon": "^8.0.0",
    "playwright": "^1.60.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
